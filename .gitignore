# No trailing slash: also ignores per-package `node_modules` *symlinks* (as
# pnpm worktrees create), which a dir-only `node_modules/` pattern misses.
node_modules
dist/
build/
coverage/
*.log
.env
.env.*
!.env.example
.DS_Store
*.local
.turbo/
studio/logs/
